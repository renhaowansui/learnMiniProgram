<!--pages/home/childComponents/recommends.wxml-->
<view class="recommends">
  <block wx:for="{{recommends}}" wx:key="index">
    <view class="recommends-item">
      <image src="{{item.image}}" onload="imageLoad" />
      <text>{{item.title}}</text>
    </view>
  </block>
</view>
